[project]
name = "paper-forge"
version = "0.1.0"
description = "Document to Academic Paper PDF Converter"
requires-python = ">=3.10"

[project.dependencies]
fpdf2 = ">=2.7.0"
langchain = ">=0.3.0"
langchain-openai = ">=0.2.0"
openai = ">=1.0.0"
requests = ">=2.28.0"
pyyaml = ">=6.0"
gradio = ">=4.0.0"
Pillow = ">=9.0.0"
